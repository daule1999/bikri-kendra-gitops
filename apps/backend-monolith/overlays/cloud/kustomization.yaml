apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bikri-prod
resources:
  - ../../base
configMapGenerator:
  - name: backend-config
    behavior: merge
    literals:
      - DB_HOST=<cloud-db-host>   # k3s bare-metal: host LAN IP
      - DB_PORT=3306
# ── DEPLOYS HAPPEN HERE ── bump newTag, commit, ArgoCD syncs.
images:
  - name: ghcr.io/<org>/vy/bikri-backend
    newTag: main
