apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  labels: { app: billing-service, tier: backend }
spec:
  replicas: 1
  selector: { matchLabels: { app: billing-service } }
  template:
    metadata:
      labels: { app: billing-service, tier: backend }
    spec:
      containers:
        - name: billing-service
          image: ghcr.io/<org>/vy/billing-service:placeholder   # tag set by overlay
          ports: [ { containerPort: 8085 } ]
          envFrom:
            - configMapRef: { name: backend-config }
            - secretRef: { name: backend-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8085 }
            initialDelaySeconds: 20
          resources:
            requests: { memory: 384Mi, cpu: 100m }
            limits: { memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
spec:
  selector: { app: billing-service }
  ports: [ { port: 8085, targetPort: 8085 } ]
