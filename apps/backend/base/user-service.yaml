apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels: { app: user-service, tier: backend }
spec:
  replicas: 1
  selector: { matchLabels: { app: user-service } }
  template:
    metadata:
      labels: { app: user-service, tier: backend }
    spec:
      containers:
        - name: user-service
          image: ghcr.io/<org>/vy/user-service:placeholder   # tag set by overlay
          ports: [ { containerPort: 8083 } ]
          envFrom:
            - configMapRef: { name: backend-config }
            - secretRef: { name: backend-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8083 }
            initialDelaySeconds: 20
          resources:
            requests: { memory: 384Mi, cpu: 100m }
            limits: { memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector: { app: user-service }
  ports: [ { port: 8083, targetPort: 8083 } ]
