apiVersion: apps/v1
kind: Deployment
metadata:
  name: sales-service
  labels: { app: sales-service, tier: backend }
spec:
  replicas: 1
  selector: { matchLabels: { app: sales-service } }
  template:
    metadata:
      labels: { app: sales-service, tier: backend }
    spec:
      containers:
        - name: sales-service
          image: ghcr.io/<org>/vy/sales-service:placeholder   # tag set by overlay
          ports: [ { containerPort: 8081 } ]
          envFrom:
            - configMapRef: { name: backend-config }
            - secretRef: { name: backend-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 20
          resources:
            requests: { memory: 384Mi, cpu: 100m }
            limits: { memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: sales-service
spec:
  selector: { app: sales-service }
  ports: [ { port: 8081, targetPort: 8081 } ]
